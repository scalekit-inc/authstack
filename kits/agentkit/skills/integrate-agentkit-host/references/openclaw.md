# AgentKit on OpenClaw

Stay on `SKILL.md` for the OpenClaw path. This file is the short host note.

## Install

```bash
clawhub install scalekit-agent-auth
```

Listing: https://clawhub.dev/skills/scalekit-agent-auth
Repo: https://github.com/scalekit-inc/openclaw-skill

Do not create a new GitHub repo.

## Env

Prefer `SCALEKIT_*`. The shipped skill also reads `TOOL_ENV_URL`, `TOOL_CLIENT_ID`, `TOOL_CLIENT_SECRET`, and `TOOL_IDENTIFIER`.

## Chat loop

The host follows the ClawHub skill `scalekit-agent-auth`.

**Done when:** `clawhub` lists `scalekit-agent-auth` and the four env names exist.
